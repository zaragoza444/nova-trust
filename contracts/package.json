{
  "name": "@nova/contracts",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "build": "hardhat compile",
    "test": "hardhat test",
    "deploy": "node --experimental-strip-types ./scripts/deploy.ts"
  },
  "devDependencies": {
    "hardhat": "^3.4.5"
  },
  "dependencies": {
    "ethers": "^6.16.0"
  }
}
